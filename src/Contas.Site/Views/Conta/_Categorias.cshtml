@using Contas.Application.ViewModels

@model ContaViewModel

@{
    var categorias = (IEnumerable<SelectListItem>) new CategoriaViewModel().Categorias();
}

<select asp-for="IdCategoria" asp-items="@categorias" class="form-control">

</select>